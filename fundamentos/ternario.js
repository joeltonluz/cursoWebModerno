//const resultado = nota => nota >= 7 ? "Aprovado" : "Reprovado";
function resultado (nota) {
    if (nota >= 7) {
        return "Aprovado"
    }
    else
    {
        return "Reprovado"
    }
}

console.log(resultado(8))
